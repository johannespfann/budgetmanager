<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- group with single selected item so only one item is highlighted in the nav menu -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_item_home"
            android:icon="@drawable/ic_home_black_24dp"
            android:title="Startseite"/>
        <item
            android:id="@+id/drawer_item_balance"
            android:icon="@drawable/pfann_appicon"
            android:title="Bilanz"/>
        <item
            android:id="@+id/drawer_item_history"
            android:icon="@drawable/ic_history_black_24dp"
            android:title="Verlauf"/>
        <item
            android:id="@+id/drawer_item_settings"
            android:icon="@drawable/ic_settings_black_24dp"
            android:title="Einstellungen"/>
        <item
            android:id="@+id/drawer_item_profile"
            android:icon="@drawable/ic_supervisor_account_black_24dp"
            android:title="Konto"/>
    </group>
</menu>